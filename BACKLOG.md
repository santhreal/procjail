# BACKLOG - procjail
DONE 2026-07-14 src/seccomp/mod.rs:258 | bug | critical | SYS_newfstatat is allowed only on x86_64. This module uses SYS_stat/SYS_lstat/SYS_open unconditionally, which do NOT exist on aarch64/riscv64. | FIXED: build_filter now gates its x86_64-specific syscall allowlist to target_arch="x86_64" and returns Err(SeccompError::UnsupportedArch) on other Linux arches, so the crate compiles on aarch64/riscv64 while failing closed. The SYS_newfstatat line is now mod.rs:268 (was 258). Tests: seccomp::tests::build_filter_is_arch_appropriate. Build: cargo +1.92.0 test --lib -- CARGO_EXIT=0, 26 passed; aarch64-unknown-linux-gnu check compiles. Full `cargo test` concurrent suite fails pre-existing (child processes SIGKILLed by earlyoom/137), not caused by this change.
src/seccomp/mod.rs:158 | bug | high | SYS_fcntl was missing from the seccomp allowlist; the default mismatch action is Errno(EPERM) (mod.rs:341), so fcntl-using operations (FD_CLOEXEC, O_NONBLOCK, File::try_clone via F_DUPFD_CLOEXEC, locks) fail under the sandbox. | FIXED: added allow_syscall(SYS_fcntl) in the FD-operations block; compiles + seccomp tests pass. NOTE: needs a dogfood run of a real fcntl-using program under procjail to confirm end-to-end. | status=done
src/process/builder.rs:321 | bug | high | build_bwrap_command only mounts work_dir read-only, missing runtime and harness_path mounts | Pass runtime and harness_path to build_bwrap_command and add --ro-bind mounts for both | status=done | FIXED: build_bwrap_command now takes harness_path and --ro-bind mounts it (validated absolute) plus the runtime binary when absolute (bare PATH names must come via readonly_mounts). Without these the tmpfs root left the sandbox unable to read/exec its own harness/runtime. Proving test process::builder::bwrap_bind_tests::bwrap_binds_harness_and_absolute_runtime asserts both --ro-bind pairs via Command::get_args(). NEEDS a real-bwrap-host dogfood for full e2e (this host lacks bwrap; the tests/integration bwrap tests are orphaned - see below).
src/detect.rs:138 | bug | medium | check_bubblewrap probe uses --ro-bind / / instead of --tmpfs / to check bubblewrap availability | Update check_bubblewrap to use --tmpfs / to match the actual sandbox filesystem configuration | status=done | FIXED (surfaced as the red audit_tests::verify_bwrap_probe_matches_actual_command, which was PRE-EXISTING, not caused by the builder fix): probe now uses --tmpfs / --proc --dev + --ro-bind /usr + --ro-bind-try /bin /lib /lib64 + /bin/echo ok, mirroring the real sandbox layout while binding the probe command's own deps (-try avoids false-negatives on merged-/usr hosts). NEEDS real-bwrap-host dogfood to confirm availability still detects true.
src/detect.rs:128 | bug | medium | check_unshare probe lacks --mount and --mount-proc flags used by build_unshare_command | Add --mount and --mount-proc flags to the check_unshare command arguments | status=done | FIXED (surfaced as red audit_tests::verify_unshare_probe_matches_actual_command, pre-existing): check_unshare probe now passes --mount-proc --mount --map-root-user, mirroring build_unshare_command, so availability detection tests the real namespace set. --mount does not hide inherited mounts so the probe echo still runs.
src/process/mod.rs:381 | silent-fallback | medium | is_alive did `try_wait().ok().flatten().is_none()`: on a try_wait Err it returned TRUE (claims alive), silently swallowing the error - a caller polling is_alive() to wait for exit would hang forever. | FIXED: explicit match; Ok(None)=alive, Ok(Some)=dead, Err logs loudly to stderr and returns false (an unwaitable child - typically ECHILD - is gone). Kept the `-> bool` signature because a trait impl (builder.rs:509) requires it, so Result<bool> would break the contract (Law 3). | status=done
src/process/mod.rs:172 | silent-fallback | medium | spawn did `if let Ok(cgroup) = CgroupV2::new(..)` with no else, so if cgroup creation FAILED the sandbox ran with NO memory/CPU/pids hardware limits and nobody was told - a fail-OPEN security control (Law 10 + "fail closed for security controls"). Write failures inside were already fail-closed; only creation failure leaked. | FIXED: added Tier-A config `require_hardware_limits: bool` (default false; builder + serde + TOML). On CgroupV2::new Err: fail closed (kill child + Err) when required, else emit a LOUD stderr warning (never silent). Proving tests defaults_to_not_requiring_hardware_limits + builder_sets_require_hardware_limits + require_hardware_limits_parses_from_toml. | status=done
src/process/mod.rs:175 | dedup | low | cleanup `let _ = child.kill(); let _ = child.wait();` duplicated ~5x on spawn error paths. | FIXED: extracted `fn kill_and_reap(child: &mut Child)`; all spawn-time error paths call it. The 4 cgroup set_*/attach checks also collapsed into one `.and_then` chain with a single kill_and_reap+Err. | status=done
src/seccomp/mod.rs:143 | bug | high | ROOT-CAUSED (was tests/integration_test.rs:44 "eof early" mystery): the pre-exec seccomp allowlist was MISSING SYS_pread64. A bare `sh <script>` reads the script file via pread64 (confirmed by stracing sh: it uses pread64, not just read); the default mismatch action is Errno(EPERM) (mod.rs:341), so sh's pread64 on the harness returned EPERM, sh could not read the script and exited 127 (exit-0 harness) or produced no output before dying ("eof early" for echo harnesses). Applied to EVERY strategy incl Strategy::None since apply_pre_exec_isolation (builder.rs:267) installs seccomp unconditionally. The `tests/_debug_pread.rs` scratch probe confirms someone hit exactly this pread64 EPERM. | FIXED: added allow_syscall(SYS_pread64) + allow_syscall(SYS_pwrite64) in the Basic-I/O block (mod.rs:143) - positioned I/O is the same capability as the already-allowed read/write, just with an explicit offset. Verified: adversarial suite 7->8 green (test_unicode_env_vars, the only exit-0 sh spawn in that suite, now passes), integration_test 5-fail->2-fail (basic_shell_execution/spawn_passes_work_dir/custom_sandbox_provider now green). The residual 2 integration_test failures are a DIFFERENT root cause (RLIMIT_NPROC, see below). | status=done
DONE 2026-07-14 src/process/builder.rs:249 | bug | high | RLIMIT_NPROC=32 ABSOLUTE cap made any multi-process sandbox unusable. apply_pre_exec_isolation previously set setrlimit(RLIMIT_NPROC, config.max_processes) directly in the pre_exec closure. | FIXED: extracted `fn nproc_limit_for_uid(max_processes: u64) -> u64` in src/process/builder.rs:249 that computes the current UID thread count + max_processes + 64; both apply_pre_exec_isolation and build_firejail_command use it, removing the debug `ps`/`eprintln!` block and moving the /proc scan out of the post-fork pre_exec closure. Added test `test_sh_can_fork_and_exec_env` in tests/integration_test.rs. | status=done
DONE 2026-07-14 tests/integration/mod.rs:296 | capability | high | bwrap strategy cannot run a bare-name runtime ("sh") in a tmpfs-root sandbox | FIXED: build_bwrap_command now --ro-bind /usr /usr and --ro-bind-try /bin /lib /lib64 before per-test mounts, so a bare-name runtime resolves inside the tmpfs root. Added unit test `process::builder::bwrap_bind_tests::bwrap_binds_system_dirs_for_bare_name_runtime`. `cargo test --lib` CARGO_EXIT=0 (27 passed). Note: the integration tests spawn real bwrap sandboxes and require unprivileged user namespaces; in this container bwrap fails with "setting up uid map: Permission denied", so those integration tests are environment-gated, not a code regression. | status=done
src/process/kill.rs:70 | silent-fallback | low | open_pidfd ignores the return value of fcntl setting FD_CLOEXEC via let _ | FIXED (fail closed): if fcntl(F_SETFD, FD_CLOEXEC) returns < 0 the fd would leak into every child (a sandbox escape vector), so it is now closed and open_pidfd returns None instead of handing back a leaky descriptor. | status=done
src/process/kill.rs:157 | silent-fallback | low | clock_ticks_per_second uses unwrap_or(100) to silently handle try_from conversion failures | FIXED: after the `ticks <= 0` guard, cast the small positive clock rate directly (`ticks as f64`, exact for CLK_TCK ~100 Hz) instead of `u32::try_from(ticks).unwrap_or(100)` which silently substituted 100 on an implausible overflow. | status=done
src/process/mod.rs:172 | silent-fallback | medium | spawn silently ignores CgroupV2::new errors and runs the process without hardware limits | DUPLICATE of the mod.rs:172 row above (already FIXED via the require_hardware_limits Tier-A config: fail-closed when required, loud stderr warning otherwise). Reconciled by dedup-on-file:line. | status=done
src/seccomp/mod.rs:273 | capability | low | SYS_mmap2 is allowed for target_arch arm but detect_arch does not recognize arm | FIXED (Law-2 dead-code removal, option b): removed the `#[cfg(target_arch = "arm")] allow_syscall(SYS_mmap2)` rule. It was unreachable dead code - mmap2 is a 32-bit-only syscall, and detect_arch (called at build_filter start, mod.rs:136) accepts only x86_64/aarch64/riscv64 and returns UnsupportedArch on 32-bit arm BEFORE any rule is added, so on arm the filter is never built and on 64-bit targets the cfg gated it out. Replaced with a NOTE explaining 64-bit-only support (64-bit arches use SYS_mmap, allowed at mod.rs:148). Behavior-neutral on every target. Chose removal over adding arm to detect_arch: 32-bit arm support would need a TargetArch::Arm variant + AUDIT_ARCH_ARM + the 32-bit syscall table, unwarranted for an unsupported target. procjail lib 21/0. | status=done
Cargo.toml:17 | deps | low | dependencies are pinned to exact versions with = preventing patch or security updates | DONE 2026-07-14: removed the leading = from dependency versions in Cargo.toml. `cargo check` passes (CARGO_EXIT=0).
DONE 2026-07-14 tests/resource_limits.rs:77 | test-gap | low | memory_limit_kills_shell_allocator wrote temporary files directly to host /tmp instead of work_dir | FIXED: replaced the 1M×4 dd loop with a single dd using the work_dir path ("$1/procjail_alloc") and bs=64M, which exceeds the 16 MiB RLIMIT_AS and fails to allocate its I/O buffer, causing the shell to exit quickly. Removed unused `std::time::Duration` and `std::thread` imports from resource_limits.rs. Test `memory_limit_kills_shell_allocator` passes (`CARGO_EXIT=0`). | status=done
DONE 2026-07-14 | src/process/builder.rs:67 | test-gap | medium | no integration tests verify that pre-exec isolation limits and seccomp are enforced for custom providers | FIXED: added `test_custom_provider_preserves_pre_exec_rlimits_and_seccomp` in `tests/integration_test.rs` using a `NoopCustomProvider` that mutates the command (sets env) and lets `apply_pre_exec_isolation` run. Harness asserts `ulimit -v` == 123MiB in KB, `ulimit -t` == 456s, `ulimit -n` == 89, and a `mkdir` syscall fails with EPERM (exit 1 + "Operation not permitted") under seccomp. `cargo +1.92.0 test -p procjail --test integration_test` passes (7 passed, CARGO_EXIT=0). | status=done
src/process/builder.rs:511 | perf | low | spawn_parent_watchdog uses a fixed-sleep loop that wakes up every 25ms wasting CPU cycles | FIXED (Law-7, Condvar): replaced the `loop { check cancel; sleep min(remaining,25ms) }` busy-poll (which woke ~40x/sec for the WHOLE timeout, e.g. 12,000 wakeups on a 300s timeout) with a new `WatchdogCancel { cancelled: Mutex<bool>, cv: Condvar }` type. The watchdog now blocks ONCE via `wait_until_cancelled_or(timeout)` (a single `Condvar::wait_timeout_while`), waking early only when `cancel()` signals or the deadline elapses - zero polling. Threaded through: SandboxedProcess.watchdog_cancel field `Arc<AtomicBool>` -> `Arc<WatchdogCancel>`, the 3 `.store(true,Release)` call sites (wait/kill/drop paths mod.rs:371/461/509) -> `.cancel()`, and the spawn call. Correctness: cancel() sets the flag under the mutex THEN notify_all (no lost wakeup since the waiter re-checks the predicate under the mutex); PoisonError::into_inner keeps it robust to a poisoned lock. Proving tests (watchdog_cancel_tests): cancel_sets_flag, wait_returns_true_immediately_when_already_cancelled (<1s, not 60s), wait_returns_false_on_timeout (>=30ms single wait), cancel_wakes_waiter_promptly_not_after_full_timeout (60s wait woken by cancel in <5s on a separate thread). | status=done
src/process/mod.rs:175 | dedup | low | cleanup calls let _ = child.kill() and wait() are duplicated 4 times under CgroupV2 setup | DUPLICATE of the mod.rs:175 row above (already FIXED via the extracted kill_and_reap helper + the cgroup checks collapsed into one .and_then chain). Reconciled by dedup-on-file:line. | status=done

DONE 2026-07-14 tests/{unit,adversarial,concurrent,integration,property,regression}/*.rs | test-gap | high | DISCOVERED: SIX test subdirs were NOT wired into any target - no tests/*.rs does `#[path] mod` for them and there were no [[test]] entries, so Cargo (which only auto-discovers top-level tests/*.rs, never subdir mod.rs) never compiled them. ~44 real tests dead: concurrent/mod.rs (6 parallel-spawn/kill stress), integration/mod.rs (~15 incl the real bwrap e2e bwrap_readonly/writable_mount + strategy matrices + toml roundtrip), property/mod.rs (9 proptests), regression/mod.rs (~14 security regressions), plus unit/mod.rs + adversarial/mod.rs which were EMPTY shells (no `pub mod` decls) so their siblings config_tests/detect_tests/process_tests/seccomp_tests.rs + adversaria | DONE 2026-07-14: Cargo.toml already has [[test]] entries for unit/adversarial/concurrent/integration/property/regression/gap and each mod.rs has pub mod declarations; verified `cargo test --test unit` and `cargo test --test adversarial` compile and run.l_tests.rs were DOUBLE-dead. | FIXED (dead coverage recovered + ONE-PLACE, mirrors blindtime consolidation): (1) added explicit [[test]] entries in Cargo.toml for all six subdirs (unit/adversarial/concurrent/integration/property/regression) + a new gap target, each pointing at tests/<kind>/mod.rs; (2) populated unit/mod.rs (config_tests+detect_tests+process_tests+seccomp_tests) and adversarial/mod.rs (adversarial_tests) with their missing `pub mod` decls; (3) folded the redundant flat top-level test_depth_{unit,adversarial,property,gap}.rs auto-targets INTO the matching mod-trees as submodules (test_depth_gap became tests/gap/{mod,depth}.rs) so each kind is ONE target. Wiring surfaced TWO API-drift bugs the never-compiled tests hid, both fixed: (a) property/mod.rs referenced `procjail::config::DEFAULT_SECRET_ENV_VARS` but `mod config` is private -> re-exported the pub const at crate root (`pub use config::{...,DEFAULT_SECRET_ENV_VARS}`, keeping the module private per the crate's one-re-export-point architecture) + repointed the 3 test refs; (b) integration/mod.rs called `.display()` on a `TempDir` (no such method) -> `.path().display()`. Compile now green. Runtime pass verification of the sandbox-spawning suites pending (some are env-gated like the pre-existing integration_test.rs:44 eof-early class). | status=done
DONE 2026-07-14 tests/_debug_{fcntl,pread,simple,spawn,spawn2,strace}.rs | organization | medium | SCRATCH DEBUG FILES auto-compiled as 6 separate test targets: _debug_strace.rs runs the `strace` binary, _debug_spawn/spawn2/simple do manual raw-Command spawn debugging, _debug_fcntl/pread assert seccomp blocking (fcntl_blocked/pread_blocked) - the latter two duplicate what tests/seccomp_blocked_syscalls.rs covers properly. All are `_`-prefixed dev scratch (Law 2: no debug scaffolding as shipped test targets) that pollute the suite with 6 junk targets, several requiring host tools (strace) that make them non-portable. | Remove the 6 _debug_*.rs after confirming fcntl_blocked/pread_blocked assertions are covered by seccomp_blocked_syscalls.rs (fold any unique seccomp-block assertion in first); the manual-spawn/strace ones are pure scratch and carry no unique coverage. | status=done

DONE 2026-07-14 src/seccomp/mod.rs:199 | bug | high | SYS_fork and SYS_vfork were missing from the seccomp allowlist; `dash` (the default `/bin/sh` on Debian/Ubuntu) uses the vfork syscall to run external commands, so `sh` harnesses running `env` etc. received EPERM and printed "Cannot fork". This was the other half of the multi-process sandbox failure originally attributed to RLIMIT_NPROC. | FIXED: added allow_syscall(SYS_fork) and allow_syscall(SYS_vfork) in src/seccomp/mod.rs:199 under Process control. `test_sh_can_fork_and_exec_env` and `test_env_stripping_prevents_secret_leakage` pass. | status=donesrc/seccomp/mod.rs:341-354 + :383-388 | security | high | Claude 2026-07-17: `block_syscall_explicit` (383-388) is a TOTAL NO-OP — its body is `let _ = syscall; let _ = rules;`. So the entire "explicitly blocked" list (ptrace, process_vm_readv/writev, mount, umount2, reboot, swapon/off, kexec_load, kexec_file_load, bpf, userfaultfd, perf_event_open at 341-354) adds ZERO rules; those syscalls are denied ONLY by the default mismatch_action `Errno(EPERM)` (361). Consequences: (1) the security-critical blocklist is DECORATIVE — if the default action is ever changed (to Allow/Log/Trace) or one of these syscalls is later added to the allowlist by mistake, the "block" provides no independent guard and the dangerous syscall silently becomes permitted; (2) the module docs (lines 11-12, 62-68) claim these syscalls "explicitly return EPERM", which the code does NOT do — a coherence violation and false-security signal; (3) the truly catastrophic syscalls (ptrace, mount, kexec, bpf, process_vm_*) get a SOFT EPERM the program can observe and branch on, rather than a hard KillProcess; (4) `perf_event_open` is listed TWICE (353-354), dead evidence the list was never reviewed. | Make blocking real and independent of the default: install an explicit per-syscall rule (Errno(EPERM) for soft-deny, or KillProcess for ptrace/mount/kexec/bpf/process_vm_* which should never be attempted by a benign child) so the blocklist enforces on its own; dedup the double perf_event_open; update the module docs to match. Proving test: after building the filter, assert each blocked syscall has an explicit rule entry (not merely absent), and a test that flipping the default to Allow still denies ptrace/mount/kexec. | status=open
src/seccomp/mod.rs:181 | security | high | Claude 2026-07-17: `SYS_ioctl` is blanket-allowed with NO argument filtering (`allow_syscall` always inserts an empty rule vec = unconditional match, 376-379 — so NO syscall in this sandbox uses argument matching, despite `SeccompRule` being imported at line 22). ioctl is a well-known seccomp escape surface: `TIOCSTI` injects characters into the controlling terminal's input queue (a sandbox escape into the parent shell), and many device drivers expose dangerous request codes. A sandbox that permits unrestricted ioctl has a materially weaker confinement than its whitelist implies. | Argument-filter ioctl by request number: at minimum deny `TIOCSTI` (and the `TIOCLINUX`/`TIOCSTI`-class terminal-injection requests); ideally allowlist only the request codes the sandboxed workload actually needs. Use `SeccompRule` with a comparator on arg1 (the request). Proving test: the built filter denies an ioctl(TIOCSTI) while permitting an allowed request (e.g. TCGETS/FIONBIO). | status=open
src/seccomp/mod.rs:225-228 + :331-334 | security | medium | Claude 2026-07-17: (a) the comment at 225 claims signal-sending is "self-only enforced by kernel with NO_NEW_PRIVS" — this is FALSE. NO_NEW_PRIVS blocks privilege escalation via exec; it does NOT restrict `kill`/`tkill`/`tgkill` targets. With SYS_kill/tkill/tgkill blanket-allowed (226-228), the sandboxed process can signal ANY process it has UID permission to signal, not just itself. (b) SYS_setuid/setgid/setresuid/setresgid (331-334) and capset (in the capget/capset block) are allowed inside the sandbox; the comment "Uid/gid mapping for user namespaces" is misleading — uid_map is written via /proc, not these syscalls — so a confined process retains the ability to change its own credentials, weakening the sandbox's credential confinement. | Correct the false NO_NEW_PRIVS comment. If cross-process signalling and in-sandbox credential changes are not required by the workload, remove SYS_kill/tkill/tgkill (or argument-restrict kill to the process's own tgid via SeccompRule) and drop the setuid family + capset from the allowlist. Proving test: assert the built filter denies kill() to an arbitrary PID and denies setuid(), or, if intentionally kept, a documented justification test. | status=open
